# 部署前整理清單

更新日期：2026-03-21

本清單用於艾苜正式版網站在部署到正式環境前的最後整理，目標是避免因環境變數、CORS、內容 seed、legacy 頁面或壞圖連結等問題，導致正式站上線後才回頭修補。

## 1. 確認部署架構

### 前端
- 平台：Vercel
- 專案目錄：`amu-readdy-code-v2`
- Build command：`npm run build`
- Output directory：`out`

### Studio
- 平台：Sanity Hosted Studio
- 專案目錄：`amu-website-formal-studio`
- 部署指令：`npx sanity deploy`

### 資料層
- Sanity project：`pnfq5r7n`
- dataset：`production`

## 2. 正式環境變數

### 前端（Vercel）
正式環境至少確認以下 env：

```bash
VITE_SANITY_PROJECT_ID=pnfq5r7n
VITE_SANITY_DATASET=production
VITE_SANITY_API_VERSION=2025-01-01
VITE_SANITY_STUDIO_URL=<正式 Studio 網址>
VITE_ENABLE_VISUAL_EDITING=false
```

補充：
- 正式站必須關閉 `VITE_ENABLE_VISUAL_EDITING`
- 若 preview 環境仍要保留視覺編輯，可在 preview env 另外設為 `true`

### Studio
正式 Studio 至少確認以下 env：

```bash
SANITY_STUDIO_PROJECT_ID=pnfq5r7n
SANITY_STUDIO_DATASET=production
SANITY_STUDIO_PREVIEW_ORIGIN=<正式前端網址>
```

補充：
- 若正式前端網址變更，`SANITY_STUDIO_PREVIEW_ORIGIN` 需一起更新

## 3. CORS 設定

正式專案需確認已加入以下 origin：
- 本地前端：`http://localhost:3000`
- 本地 Studio：`http://localhost:3333`
- 正式前端網址
- 正式 Studio 網址（若有需要）

注意：
- 新 Sanity project 不會繼承舊 project 的 CORS 設定
- 正式部署前要再確認一次，不要只相信本地已通

## 4. Build 驗證

### 前端
在 `amu-readdy-code-v2` 執行：

```bash
npm run type-check
npm run lint
npm run build
```

### Studio
在 `amu-website-formal-studio` 執行：

```bash
npm run build
```

通過標準：
- `type-check` 通過
- `lint` 通過
- `build` 通過
- 沒有新增未處理的 runtime warning / schema error

## 5. Seed 與內容確認

正式部署前，至少確認以下文件已存在於 Sanity：
- `siteSettings`
- `homePage`
- `featuredTreatmentsPage`
- `featuredTreatmentDetail`（目前 6 筆）
- `aboutPage`
- `insurancePage`
- `casesPage`
- `caseArticle`（目前 5 筆）
- `teamPage`
- `doctorProfile`（目前 3 筆）
- `healthEducationPage`
- `healthEducationArticle`（目前 10 筆）

若內容有重新整理過，重新 seed 的指令清單：

```bash
cd '/Users/alex332233/Documents/projects/website/艾苜網站readdyCode/amu-website-formal-studio'
npm run seed:home
npm run seed:featured
npm run seed:about
npm run seed:insurance
npm run seed:cases
npm run seed:team
npm run seed:health-education
npm run seed:site-settings
```

## 6. 正式站 Overlay 與 Preview 驗證

### 正式站
- 正式前端不可出現 Visual Editing overlay
- 一般使用者不應看到任何編輯框或 Studio 相關 UI

### Studio / Presentation
- 可以正常開啟正式站 preview
- 能從畫面點到主要可編欄位
- Header / Footer、首頁、特色療程、健保項目、真實見證、醫師團隊、衛教資訊都能正常對應

## 7. 上線前人工驗收

至少人工檢查以下頁面：
- `/`
- `/about`
- `/insurance`
- `/featured-treatments`
- `/featured-treatments/facial`
- `/featured-treatments/growth`
- `/cases`
- `/cases/1`
- `/team`
- `/health-education`
- `/health-education/1`

每頁至少確認：
- 頁面可正常打開
- Header / Footer 內容正確
- CTA 可點擊
- 主要圖片可正常顯示
- 手機版 navbar 正常
- 沒有明顯 layout 爆版

## 8. 壞圖與壞連結檢查

這次實作已遇過 Readdy 圖片 404，因此正式部署前要再做一次人工檢查：
- 首頁 Hero / gallery / testimonials
- 真實見證案例卡與內頁封面
- 醫師照片
- 特色療程圖片
- 衛教資訊文章封面與內文圖片

若圖片網址來自 Readdy，需特別注意：
- `public.readdy.ai` 或 `readdy.ai/api/search-image` 連結可能之後失效
- 若某張圖已知不穩，優先替換成正式資產來源

## 9. Legacy 頁面與正式導覽確認

目前 `treatments` 頁仍視為 legacy candidate，不納入正式版主線。

部署前需確認：
- Navbar 沒有導向 legacy 頁面
- Footer 沒有誤連到 legacy 頁面
- sitemap / SEO / 導覽不會把 legacy 頁面帶進正式站

參考文件：
- [正式版疑似Legacy頁面盤點](/Users/alex332233/Documents/projects/website/艾苜網站readdyCode/docs/正式版疑似Legacy頁面盤點.md)

## 10. 權限與交接確認

部署前要先決定：
- 客戶是否立即進 Studio
- 若要進 Studio，給什麼角色
- 若暫時不給客戶編輯權限，內部由誰維護內容

補充：
- 若採 Free 方案，角色能力較有限
- 若需要更細緻的 Editor / Developer 權限，需再評估是否升級 Sanity 方案

## 11. 建議部署順序

1. 確認正式 env
2. 確認 CORS
3. 跑前端與 Studio build
4. 確認 seed 完整
5. 人工驗收主要頁面
6. 部署前端到 Vercel
7. 部署 Studio 到 Sanity Hosted Studio
8. 再次確認正式站不顯示 overlay
9. 最後檢查 Studio Presentation 是否可正常預覽正式站

## 12. 目前刻意延後的項目

以下項目目前不應阻擋正式部署，但要保留後續規劃：
- 衛教資訊文章底部 FAQ 區塊（待設計定稿）
- 關於艾苜 `FullscreenSlider` 是否納入 CMS
- 分院頁 / 分院專屬 Line@ 邏輯
- `treatments` legacy 頁面是否後續刪除或保留

## 13. Vercel 第一次 CLI 部署的實際注意事項

這次已實際用 `npx vercel --yes` 驗證過第一輪部署流程，需注意以下幾點：

### 登入與權限
- 若本機既有 Vercel token 已失效，CLI 會直接報：
  - `The specified token is not valid. Use vercel login to generate a new token.`
- 這時需先執行：

```bash
npx vercel login
```

- 依裝置碼流程到瀏覽器授權後，再重新執行部署

### Preview 網址不是自動公開
- 這次 `npx vercel --yes` 成功後拿到的是：
  - `Preview URL`
- 若專案啟用了 Vercel Authentication / Deployment Protection，未登入 Vercel 的瀏覽器（例如無痕）會被導向 Vercel login
- 這代表：
  - CLI deploy 成功不等於外部客戶一定能直接開啟 preview 連結
  - 若要給外部人員預覽，需再確認 Vercel 專案的 deployment protection 設定

### Output directory 不能只靠自動偵測
- 這次 Vercel 雖然自動辨識為 Vite，但預設仍去找 `dist`
- 實際專案 build 輸出是 `out`
- 若未明確設定，會在部署時出現：
  - `No Output Directory named "dist" found after the Build completed`
- 這次處理方式：
  - 在前端專案新增 `vercel.json`
  - 明確指定：

```json
{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "out"
}
```

### 先用 CLI 拿網址，再回填到 Studio 是可行的
- 這次已驗證：
  - `vercel` CLI 會在 stdout 直接輸出部署網址
- 因此可以先：
  1. 部署前端到 Vercel
  2. 取得 preview / production URL
  3. 再回填到 `SANITY_STUDIO_PREVIEW_ORIGIN`
- 這條流程後續可程式化，不一定要手動複製貼上

## 14. Vercel Git Integration 與正式部署的補充經驗

### Root Directory 設定後，CLI 的執行目錄也要跟著改
- 這次把 Vercel project 的 `Root Directory` 設成：
  - `amu-readdy-code-v2`
- 設定完成後，若仍在前端子目錄內執行：

```bash
npx vercel --prod --yes
```

- Vercel 會再額外套一次 root directory，變成去找：
  - `.../amu-readdy-code-v2/amu-readdy-code-v2`
- 這次實際錯誤是：
  - `The provided path ".../amu-readdy-code-v2/amu-readdy-code-v2" does not exist`
- 正確做法：
  - 設好 `Root Directory` 後，CLI 應改在 repo 根目錄執行

### Git connect 可以先用 CLI 嘗試，但 repo 授權仍可能要回 Dashboard 完成
- 這次 `npx vercel git connect` 已成功連到既有 project：
  - `alex332233s-projects/amu-readdy-code-v2`
- 但在連接 GitHub repo 時仍出現：
  - `Failed to connect alex332233/readdy-autocopilot to project`
- 根因是 Vercel 尚未取得該 GitHub repo 的授權
- 這種情況下，最穩的做法仍是：
  1. 到 Vercel Dashboard 完成 GitHub repo 授權與連接
  2. 再確認 `Root Directory = amu-readdy-code-v2`

### 第一次用 CLI 寫入 Vercel env 時，要留意值是否被帶入額外字元
- 這次 production 網站初次打開時，console 出現：
  - ``projectId can only contain only a-z, 0-9 and dashes``
- 根因不是 `pnfq5r7n` 本身有問題，而是先前 CLI 寫入 env 時，值很可能帶入了引號或其他多餘字元
- 這次處理方式：
  - 使用 `vercel env add ... --force --value <clean-value> --yes` 重新覆蓋 production env
  - 再重新執行 production deploy
- 建議：
  - 第一次用 CLI 寫 env 後，若 production 行為異常，優先懷疑 env 值格式而不是前端邏輯

### 正式網域上線後，Sanity CORS 也必須同步加入 production domain
- 這次 production 重新 deploy 後，網站仍報：
  - `blocked by CORS policy`
- 根因是 Sanity 專案的 CORS 白名單只有：
  - `localhost`
  - preview domain
- 尚未包含正式 Vercel domain
- 這次補上的正式網域：
  - `https://amu-readdy-code-v2.vercel.app`
  - `https://amu-readdy-code-v2-r5f0crtoc-alex332233s-projects.vercel.app`
- 建議：
  - 每次新增正式 alias、preview 網域或自訂網域時，都要同步檢查 Sanity CORS

### 這次已驗證通過的 production 流程
1. 設定 Vercel `Root Directory`
2. 補齊 production env
3. 在 repo 根目錄執行：

```bash
npx vercel --prod --yes
```

4. 取得正式網址與 alias
5. 將正式 Vercel domain 加入 Sanity CORS
6. 強制重整正式站，確認不再有：
   - `projectId` 格式錯誤
   - `blocked by CORS policy`
